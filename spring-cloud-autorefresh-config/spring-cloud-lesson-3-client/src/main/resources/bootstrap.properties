

# 远程属性的应用程序名称  默认是application名称
spring.cloud.config.name=consumer

# 配置分支名称
spring.cloud.config.label=master

# 环境
spring.cloud.config.profile=test

# 配置服务器的地址
spring.cloud.config.uri=http://127.0.0.1:9090